# API Configuration
# Development API URL (local backend)
VITE_API_URL=http://localhost:5000/api

# Production API URL will be set during deployment
# For production, you can either:
# 1. Use relative URL: VITE_API_URL=/api (if frontend and backend are on same domain)
# 2. Use full production URL: VITE_API_URL=https://api.yourdomain.com/api